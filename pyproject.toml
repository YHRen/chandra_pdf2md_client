[project]
name = "chandra-mcp-server"
version = "0.1.0"
description = "MCP server for PDF to Markdown conversion using Chandra vision-language model"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "filetype>=1.2.0",
    "markdownify>=1.2.2",
    "openai>=1.106.1",
    "pillow>=11.3.0",
    "pypdfium2>=5.0.0",
    "six>=1.17.0",
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
chandra-mcp = "chandra_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chandra_mcp"]

[tool.uv]
package = true
